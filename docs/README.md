# Documentation

This directory contains the GitHub Pages documentation for the ArUco Marker Generator.

The documentation is automatically built and deployed using Jekyll with the Minima theme.

## Local Development

To preview the documentation locally:

```bash
cd docs
bundle install
bundle exec jekyll serve
```

Then visit `http://localhost:4000/aruco-laser-generator/`
